let checkName = (firstName, lastName) => {
	if (firstName === 'sajal' && lastName === 'sharma') {
		console.log("You are sajal sharma");
	} else {
		console.log("You are not sajal sharma");
	}
}

checkName('sajal', 'sharma');